import fade from './modules/fadeScroll.js'


new fade({
    tag: '.fade_left',
    direction: 'left',
    overflow: true
})

new fade({
    tag: '.fade_right',
    direction: 'right',
    overflow: true
})

new fade({
    tag: '.fade_up',
    direction: 'up',
    overflow: true
})

new fade({
    tag: '.fade_down',
    direction: 'down',
    overflow: true
})
